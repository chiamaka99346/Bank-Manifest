apiVersion: v1
kind: Service
metadata:
  name: bank-frontend-service
  namespace: banking-app
spec:
  selector:
    app: bank-frontend  # This should match deployment labels
  ports:
  - port: 3000
    targetPort: 3000
  type: ClusterIP
